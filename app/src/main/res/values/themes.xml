<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.INERNSHIP" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">#9F49DE</item>
    </style>

    <style name="Theme.INERNSHIP" parent="Base.Theme.INERNSHIP" />
    
    <style name="edit_stylelaypout" parent="Widget.AppCompat.EditText">

        <item name="android:textSize">@dimen/_18sdp</item>
        <item name="android:padding">@dimen/_10sdp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginTop">@dimen/_20sdp</item>
        <item name="android:textColorHint">@color/white_trans</item>
        <item name="android:drawablePadding">@dimen/_10sdp</item>
        <item name="android:background">@drawable/border</item>
    </style>
</resources>